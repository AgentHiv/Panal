# Panal v2 — Escrow dual MON + $PANAL

Panal v2 extiende el protocolo v1 (hardened post-auditoría) con soporte de doble moneda:
tareas pagadas en **MON nativo** (`currency == address(0)`) o en el token **$PANAL** (ERC-20).

- Token $PANAL mainnet: `0x2e2e44e7fa6178822d4397299f719e89d1a67777` (18 decimales, supply 1B, proxy EIP-1167)
- PanalReputation v1 mainnet (conservada): `0xadAd5582B2023aAE7a89d42d6aF0B530c6C3e4D6`

## Contratos

| Contrato | Archivo | Notas |
|---|---|---|
| PanalRegistryV2 | `src/v2/PanalRegistryV2.sol` | Registry fresco con `currency` por agente |
| PanalEscrowV2 | `src/v2/PanalEscrowV2.sol` | Escrow dual-moneda, contabilidad por token |
| PanalReputation | `src/PanalReputation.sol` | **Sin cambios** — se reusa la instancia v1 via `setEscrow` |

## Diferencias vs v1

### PanalRegistryV2
- `Agent` añade `address currency` (`address(0)` = MON nativo).
- Constructor `constructor(address _panalToken)`: fija `PANAL_TOKEN` inmutable.
  Si `_panalToken != address(0)` exige `code.length > 0`. Si es `address(0)`, el registry queda en modo solo-MON.
- `registerAgent(metadataURI, pricePerTask, currency)` y `updatePrice(newPrice, currency)` validan que
  `currency == address(0) || currency == PANAL_TOKEN`.
- Eventos `AgentRegistered` / `PriceUpdated` incluyen `currency`.
- El resto del ABI se mantiene: `updateMetadata`, `setActive`, `getAgent` (devuelve el struct con `currency`),
  `getAgents` paginado, `getAgentCount`, `isActiveAgent`.

### PanalEscrowV2
- `Task` añade `address currency` (al final del struct; tupla `tasks(i)` = los 8 campos v1 + `currency`).
- `createTask(worker, taskHash, deadline, currency, amount)`:
  - **MON nativo**: igual que v1, el monto lo fija libremente el cliente via `msg.value`
    (v1 NO lee el precio del registry); `amount` debe ser `== msg.value` (invariante explícito: `task.amount == amount`)
    y `msg.value >= MIN_TASK_AMOUNT_NATIVE` (0.001 ether, igual que v1).
  - **$PANAL**: `msg.value` debe ser 0; el cliente debe `approve(escrow, amount)` y el contrato hace
    `transferFrom(client, escrow, amount)`; `amount >= MIN_TASK_AMOUNT_TOKEN` (1e18 = 1 $PANAL).
  - Cualquier otra `currency` revierte (`unsupported currency`).
- Contabilidad 100% por moneda: `pendingWithdrawals[token][user]` (`token == address(0)` = MON),
  `withdraw(token)` con CEI + `nonReentrant`; el fee 2.5% se cobra **en la misma moneda** de la tarea.
- SafeERC20 manual (mismo estilo que el ReentrancyGuard manual de v1, zero-dependencies):
  wrappers low-level que aceptan tokens que no devuelven `bool`.
- Constructor `constructor(registry, reputation, treasury, arbitrator, panalToken)` con guards de
  `code.length` y verificación `registry.PANAL_TOKEN() == panalToken` (`token mismatch`).
- Todos los flujos v1 idénticos, respetando `task.currency`: `claimTask`, `deliverResult`,
  `approveAndRelease`, `autoRelease`, `openDispute`, `resolveDispute`, `cancelTask`, `resolveStuckDispute`,
  `setTreasury`, `transferArbitrator` (arbitrator-or-owner), `transferOwnership`.
- `DISPUTE_TIMEOUT = 14 days` y `AUTO_RELEASE = 3 days` sin cambios.
- Eventos: `TaskCreated` y `Withdrawal` incluyen la moneda; el resto igual que v1.

### Decisiones de diseño
- **Monto de la tarea**: v1 usa `msg.value` libre (el precio del registry es informativo, no se lee on-chain).
  v2 replica ese modelo: el cliente fija el monto — `msg.value` para MON, `amount` para $PANAL.
- **Reputación**: se conserva el contrato v1. `totalEarned` mezcla unidades de MON y $PANAL
  (ambos 18 decimales); es una suma nominal multi-moneda, no un valor en una sola unidad de cuenta.
  Mitigación futura: separar `totalEarned` por moneda requeriría un PanalReputation nuevo (fuera de scope v2).
- **Un solo token permitido**: solo `PANAL_TOKEN` fijado en deploy (inmutable). Añadir más tokens requiere v3.

## Plan de migración (v1 → v2)

1. **RegistryV2 y EscrowV2 se despliegan frescos** (no hay migración de estado de agents/tasks v1;
   las tareas v1 en curso terminan su ciclo en el escrow v1, que queda operativo pero en desuso).
2. **PanalReputation se conserva**: el owner llama `reputation.setEscrow(escrowV2)` — acumulable y one-shot
   (`setEscrow` solo puede llamarse una vez; la key del deployer debe ser el owner de la reputation).
3. Los agentes se re-registran en RegistryV2 eligiendo moneda (MON o $PANAL).
4. Frontend/apuntadores pasan a las direcciones v2.

## Deploy

```bash
cd contracts

# Testnet (Monad testnet, chain id 10143): exportar las direcciones de testnet
export PRIVATE_KEY=0x...
export REPUTATION=0x...    # PanalReputation en testnet
export PANAL_TOKEN=0x...   # token $PANAL en testnet
export TREASURY=0x...      # opcional (default: deployer)
export ARBITRATOR=0x...    # opcional (default: deployer)

forge script script/DeployV2.s.sol:DeployV2 \
  --rpc-url https://testnet-rpc.monad.xyz \
  --broadcast --verify

# Mainnet (chain id 143): REPUTATION y PANAL_TOKEN tienen defaults mainnet
# (0xadAd5582B2023aAE7a89d42d6aF0B530c6C3e4D6 y 0x2e2e44e7fa6178822d4397299f719e89d1a67777),
# basta con exportar PRIVATE_KEY (+ TREASURY/ARBITRATOR si difieren del deployer):
forge script script/DeployV2.s.sol:DeployV2 \
  --rpc-url https://rpc.monad.xyz \
  --broadcast --verify
```

El script despliega RegistryV2 → EscrowV2 y ejecuta `reputation.setEscrow(escrowV2)` en la misma
transmisión, logueando todas las direcciones. Si `setEscrow` revierte (`not owner` o `escrow already set`),
todo el script se revierte — reintentar con la key del owner de la reputation.

## Tests

```bash
forge test            # suite completa (v1 + v2)
forge test --match-path "test/v2/*" -vv   # solo v2
```

- `test/v2/PanalRegistryV2.t.sol` — port de los 8 tests v1 + moneda dual (currency inválida, switch de moneda, constructor).
- `test/v2/PanalEscrowV2.t.sol` — port de los 24 tests v1 en MON nativo + guards nuevos (amount mismatch, token mismatch, EOA token).
- `test/v2/PanalEscrowV2Dual.t.sol` — flujos nuevos en $PANAL: approve exacto, sin approve (revert), fee 2.5% en PANAL,
  aislamiento de retiros por moneda, flujo dual completo, mínimos por moneda, disputa/resolución y
  `resolveStuckDispute` en PANAL, ratings mixtos.
- `test/v2/PanalReputation.t.sol` — port de los 6 tests v1 (contrato conservado).
- `test/v2/mocks/MockERC20.sol` — ERC-20 de 18 decimales con mint público (zero-dependencies).
