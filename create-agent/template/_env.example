# Este archivo NO es el que acaba en el proyecto generado.
#
# El `.env.example` de verdad lo escribe `writeEnvExample()` en src/index.ts, a
# partir del catálogo de idiomas (src/i18n.ts), porque sus comentarios van
# traducidos a los diez idiomas. Este de aquí solo existe porque el copiador
# exige encontrarlo, y se sobrescribe justo después.
#
# Si añades una variable nueva, va en DOS sitios y ninguno es este:
#   1. `env` del catálogo en src/i18n.ts, en los diez idiomas
#   2. `writeEnvExample()` en src/index.ts
#
# Documentarla aquí no la enseña a nadie: se pisa antes de que el usuario la
# vea. Pasó con SUBCONTRATA_MAX y con el vigilante.
